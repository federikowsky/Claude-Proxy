---
phase: 03-project-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - llm_proxy/__init__.py
  - llm_proxy/__main__.py
  - llm_proxy/main.py
  - llm_proxy/infrastructure/config.py
  - config/llm-proxy.yaml
  - config/llm-proxy.example.yaml
  - config/llm-proxy-test.yaml
  - README.md
autonomous: true
requirements: [R-07]

must_haves:
  truths:
    - "All source and test files import from llm_proxy, not claude_proxy"
    - "Config loads from config/llm-proxy.yaml by default"
    - "Environment variable prefix is LLM_PROXY__"
    - "Config override env var is LLM_PROXY_CONFIG"
    - "CLI entry point llm-proxy invokes llm_proxy.__main__:main"
    - "All 312+ tests pass under the new package name"
  artifacts:
    - path: "llm_proxy/"
      provides: "Renamed package directory"
    - path: "config/llm-proxy.yaml"
      provides: "Renamed default config file"
    - path: "config/llm-proxy.example.yaml"
      provides: "Renamed example config file"
    - path: "config/llm-proxy-test.yaml"
      provides: "Renamed test config file"
    - path: "pyproject.toml"
      provides: "Updated package metadata and entry point"
  key_links:
    - from: "llm_proxy/infrastructure/config.py"
      to: "config/llm-proxy.yaml"
      via: "DEFAULT_CONFIG_PATH"
      pattern: "DEFAULT_CONFIG_PATH.*llm-proxy"
    - from: "llm_proxy/infrastructure/config.py"
      to: "LLM_PROXY__"
      via: "ENV_PREFIX constant"
      pattern: "ENV_PREFIX.*LLM_PROXY__"
    - from: "pyproject.toml"
      to: "llm_proxy/__main__:main"
      via: "project.scripts entry point"
      pattern: "llm-proxy.*llm_proxy"
---

<objective>
Rename the entire project from claude-proxy to llm-proxy: package directory, all imports,
config files, environment variables, pyproject.toml, and documentation.

Purpose: The project now supports multiple LLM protocols (Anthropic + OpenAI), not just
Claude Code. The name must reflect this.
Output: Fully renamed codebase with all 312+ tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-project-rename/03-CONTEXT.md
@pyproject.toml
@claude_proxy/infrastructure/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package directory and update pyproject.toml</name>
  <files>pyproject.toml, llm_proxy/ (renamed from claude_proxy/)</files>
  <action>
1. Rename the package directory:
   ```
   mv claude_proxy llm_proxy
   ```

2. Update `pyproject.toml`:
   - `name = "llm-proxy"`
   - `description = "Multi-protocol LLM reverse proxy with dual ingress (Anthropic Messages + OpenAI Chat Completions) and multi-provider upstream routing."`
   - `[project.scripts]`: `llm-proxy = "llm_proxy.__main__:main"`
   - `[tool.hatch.build.targets.wheel]`: `packages = ["llm_proxy"]`
  </action>
  <verify>
    <automated>ls llm_proxy/__init__.py && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['name']=='llm-proxy'; assert d['project']['scripts']['llm-proxy']=='llm_proxy.__main__:main'; print('OK')"</automated>
  </verify>
  <done>Package directory is llm_proxy/, pyproject.toml references llm_proxy everywhere, old claude_proxy/ directory no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk-rename all references across source, tests, and config</name>
  <files>llm_proxy/**/*.py, tests/**/*.py, config/*.yaml, README.md</files>
  <action>
1. Update all Python imports and string references in source and test files.
   Exclude `.git/` and `.planning/` (historical artifacts keep original names):
   ```
   find . -path ./.git -prune -o -path ./.planning -prune -o -name '*.py' -print0 | xargs -0 sed -i '' 's/claude_proxy/llm_proxy/g'
   ```

2. Update hyphenated references in Python files (comments, docstrings, URLs):
   ```
   find . -path ./.git -prune -o -path ./.planning -prune -o -name '*.py' -print0 | xargs -0 sed -i '' 's/claude-proxy/llm-proxy/g'
   ```

3. Rename config files:
   ```
   mv config/claude-proxy.yaml config/llm-proxy.yaml
   mv config/claude-proxy.example.yaml config/llm-proxy.example.yaml
   mv config/claude-proxy-test.yaml config/llm-proxy-test.yaml
   ```

4. Update references inside config YAML files:
   ```
   find config -name '*.yaml' -print0 | xargs -0 sed -i '' 's/claude-proxy/llm-proxy/g; s/claude_proxy/llm_proxy/g; s/CLAUDE_PROXY/LLM_PROXY/g'
   ```

5. Update `llm_proxy/infrastructure/config.py` specifically — verify these three constants:
   - `ENV_PREFIX = "LLM_PROXY__"` (was `CLAUDE_PROXY__`)
   - `DEFAULT_CONFIG_PATH = Path("config/llm-proxy.yaml")` (was `claude-proxy.yaml`)
   - `os.getenv("LLM_PROXY_CONFIG", ...)` (was `CLAUDE_PROXY_CONFIG`)

6. Update `README.md`: replace all occurrences of `claude-proxy`, `claude_proxy`,
   `CLAUDE_PROXY`, `Claude Proxy`, `Claude-Proxy` with the corresponding `llm-proxy`
   / `llm_proxy` / `LLM_PROXY` / `LLM Proxy` / `LLM-Proxy` variants.

7. Verify zero old-name references remain in active code:
   ```
   grep -r "claude_proxy\|claude-proxy\|CLAUDE_PROXY\|Claude.Proxy" \
     --include='*.py' --include='*.yaml' --include='*.toml' --include='*.md' . \
     | grep -v '.planning/' | grep -v '.git/'
   ```
   This MUST return empty. If any matches remain, fix them.
  </action>
  <verify>
    <automated>grep -rc "claude_proxy\|claude-proxy\|CLAUDE_PROXY" --include='*.py' --include='*.yaml' --include='*.toml' . | grep -v '.planning/' | grep -v ':0$' | grep -v '.git/' | wc -l | tr -d ' ' | xargs -I{} test {} -eq 0 && echo "OK: no old references"</automated>
  </verify>
  <done>Zero occurrences of claude_proxy / claude-proxy / CLAUDE_PROXY in any .py, .yaml, or .toml file outside .planning/.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify rename integrity</name>
  <files>tests/**/*.py</files>
  <action>
1. Run the full test suite:
   ```
   python -m pytest tests/ -x -v
   ```

2. If any tests fail due to missed renames (hardcoded config paths, env var names in
   test fixtures, import paths in mocks or patches), fix the specific references and
   re-run until all tests pass.

3. Quick smoke test of the module entry point:
   ```
   python -c "from llm_proxy.main import create_app; print('import OK')"
   ```

4. Verify README coherence — scan for broken or mixed naming.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All 312+ tests pass. Module import works. No references to old project name in active code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a purely mechanical rename operation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | config.py env vars | accept | Env var prefix rename is 1:1 substitution; no new attack surface |
</threat_model>

<verification>
1. `grep -r "claude_proxy\|claude-proxy\|CLAUDE_PROXY" --include='*.py' --include='*.yaml' --include='*.toml' . | grep -v '.planning/'` returns empty
2. `python -m pytest tests/ -x` passes all 312+ tests
3. `python -c "from llm_proxy.main import create_app; print('OK')"` succeeds
4. `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['name']=='llm-proxy'"` succeeds
</verification>

<success_criteria>
- Package directory is `llm_proxy/` (no `claude_proxy/` exists)
- All imports reference `llm_proxy`
- Config files are `config/llm-proxy*.yaml`
- Env prefix is `LLM_PROXY__`
- Config path override is `LLM_PROXY_CONFIG`
- CLI entry point is `llm-proxy`
- All 312+ tests pass
- README references llm-proxy consistently
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-rename/03-01-SUMMARY.md`
</output>

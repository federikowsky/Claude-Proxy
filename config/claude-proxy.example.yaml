server:
  host: 127.0.0.1
  port: 8082
  log_level: info
  request_timeout_seconds: 120
  # Dev: validation detail logs, stream/complete start lines, and HTTP request/response body previews (logger claude_proxy.http_debug).
  debug: false

routing:
  default_model: anthropic/claude-sonnet-4
  fallback_model: anthropic/claude-sonnet-4

bridge:
  compatibility_mode: transparent
  emit_usage: true
  passthrough_request_fields: []

providers:
  openrouter:
    enabled: true
    base_url: https://openrouter.ai/api/v1
    api_key_env: OPENROUTER_API_KEY
    connect_timeout_seconds: 10
    read_timeout_seconds: 120
    write_timeout_seconds: 20
    pool_timeout_seconds: 10
    max_connections: 100
    max_keepalive_connections: 20
    app_name: claude-proxy
    app_url: null
    debug_echo_upstream_body: false

models:
  anthropic/claude-sonnet-4:
    provider: openrouter
    enabled: true
    supports_stream: true
    supports_nonstream: true
    supports_tools: true
    supports_thinking: true
    thinking_passthrough_mode: full
    provider_quirks: {}

  openai/gpt-4.1-mini:
    provider: openrouter
    enabled: true
    supports_stream: true
    supports_nonstream: true
    supports_tools: true
    supports_thinking: true
    thinking_passthrough_mode: native_only
    provider_quirks: {}
